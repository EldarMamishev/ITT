import { RequestPersonIdFamilyName } from './request-person-id-family-name';

describe('RequestPersonIdFamilyName', () => {
  it('should create an instance', () => {
    expect(new RequestPersonIdFamilyName()).toBeTruthy();
  });
});
